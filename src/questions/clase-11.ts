//Crea una funcion que tome el lado de un cuadrado como parametro y calcule su perímetro
//de ese cuadrado. El resultado debe ser el perímetro calculado.

function calcularPerimetro(lado: number) {
  return lado * 4;
}

const perimetro = calcularPerimetro(4);
console.log(perimetro + "m²");
