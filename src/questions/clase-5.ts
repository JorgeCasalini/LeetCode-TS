//Crie una funcion que recibe un número y retorna su antecesor. Por ejemplo:
//si el número es  la funcion debe retornar 4.

function antecesor(n1: number) {
  return n1 - 1;
}

const numero = antecesor(10);
console.log(numero);
