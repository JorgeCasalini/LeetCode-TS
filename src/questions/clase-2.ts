//La funcion recebe un parametro del tipo number y retorna el resultado de la
//  multiplicacion por 2.

function multiPor2(number: number) {
  return number * 2;
}

const multiplicacion = multiPor2(74);
console.log(multiplicacion);
